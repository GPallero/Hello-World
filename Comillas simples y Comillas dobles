# Para imprimir una cadena de caracteres utilizamos (") o (') para encerrar dicha cadena.
# Si queremos que una(s) (") o una(s) (') sean impresas como parte de nuestra cadena tenemos dos formas de hacerlo:

# 1- Utilizamos como delimitador el tipo de comilla que no queremos que sea parte de nuestra cadena, de esta forma el otro tipo de comilla sera impreso como parte del texto.

print('"Estoy"\n""aprendiendo""\n"""Python"""')
print()

# 2- Utilizando el caracter de escape para indicar que el siguiente caracter debe ser tenido en cuenta como tal, en nuestro caso (") o (')

print("\"Estoy aprendiendo Python\"")

#Guillermo A. Pallero
